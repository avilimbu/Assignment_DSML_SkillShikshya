>>Assignments

1. Create a Python program using at least 10 string methods and show their outputs with examples. Try methods like upper(), split(), replace(), find(), and strip().
2. Take a sentence from the user and perform different string operations on it such as converting case, counting words, checking digits/alphabet, and replacing words. Print all results clearly with labels.